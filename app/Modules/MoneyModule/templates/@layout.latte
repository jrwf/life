<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">

    <title>{ifset title}{include title|stripHtml} | {/ifset}Nette Web</title>
    <link rel="stylesheet" href="{$baseUrl}/css/main.css">
    <link rel="stylesheet" href="{$baseUrl}/css/money/money.css">
</head>

<body>

<nav class="main">
    <li n:if="$user->loggedIn"><a n:href="Homepage:default">Hlavni strana</a></li>
    <li n:if="$user->loggedIn"><a n:href="Money:default">Penize</a></li>
</nav>

<ul class="navig">
    {if $user->loggedIn}
        <li><a n:href="Sign:out">Odhlásit</a></li>
    {else}
        <li><a n:href="Sign:in">Přihlásit</a></li>
    {/if}
</ul>

<div class="content">
    <div n:foreach="$flashes as $flash" n:class="flash, $flash->type">{$flash->message}</div>
    {include content}
</div>

{block scripts}
    <script src="https://nette.github.io/resources/js/3/netteForms.min.js"></script>
{/block}
</body>
</html>
